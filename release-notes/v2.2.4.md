# v2.2.4 Source Release

发布日期：2026-06-15

本次为 GitHub 源码更新：同步公开源码、文档和功能清单，不打包 Electron，不生成安装包 / blockmap / `latest.yml`，也不发布 GitHub Release 自动更新资产。

## 更新内容

- 新增创作台背景层：左侧控制轨可进入画布创作台背景编辑，支持从资源库放置图片、调整透明度、边框、等比缩放、旋转和层级，完成后图片作为节点下方的个性化背景，不阻挡节点编辑与连线。
- RH 工具箱升级为可复用能力中间层：上传素材和输出素材的图像节点改为左侧 RH 能力轨，内置 `抠图` 与 `4K` 小按钮，分别调用 RH 高清抠图和 `image-upscale-4k` 高清放大4K 并输出新素材；图像能力预设保留 `image.cutout`、`image.upscale`、`image.expand`，后续高清化、扩图等能力可复用同一入口。
- RH 图像批处理支持串行队列、默认 3 次尝试、取消和部分成功输出；单张失败不会丢掉已完成结果，失败项会以 `failedItems` 回传。
- 修复 RH 停止只停本地的问题：RunningHub、RH工具箱、RH超市和上传 / 输出素材左侧 RH 能力轨在停止时都会调用 RunningHub `/task/openapi/cancel`，并等待远端取消成功后才清本地状态；提交中点停止会在 taskId 返回后立即取消后台任务，query/cancel 复用提交时的 API Key。取消接口若返回 HTML，会提示本地后端未命中 / 需要重启，而不是显示裸 `Unexpected token '<'`。
- 修复 RH 高清放大4K 指向正确 WebAppId `2066353965784199169` 并默认 `plus` 实例；开发环境会合并制作器最新草稿，同时按标题/WebApp/ID 清理旧重复草稿，RH 工具箱列表和筛选结果不会再显示两个同名工具。
- AI 去水印节点补齐停止功能，追加隐形处理时可中止长任务；当用户选择 CUDA 但随包 PyTorch 为 CPU-only 时，会回退/提示兼容原因，避免长堆栈误导。
- 上传素材和输出素材节点新增媒体元信息展示：图片显示分辨率，视频显示分辨率和时长，音频显示时长，并保持紧凑不破坏节点 UI。
- ComfyUI 应用制作器继续修复控件手势边界和参数映射稳定性，避免点击标签/控件无响应或把参数改乱。
- 公开版本同步到 `2.2.4`：`package.json`、`package-lock.json`、`vite.config.ts/js`、`electron/main.cjs`、`backend/src/config.js`、`features.json`、`README.md` 与本 release note 同步。

## 边界说明

- 本次只推公开源码，不运行 `npm run dist:release`、`npm run dist`、`npm run release:github` 或 `npm run release:verify`。
- 公开提交继续排除 `skill.md`、`roadmap.md`、`local-private/**`、`hermes-agent/**`、Electron 缓存目录、维护者本地制作工具源码和任何私有商业能力 / 分组令牌实现。
- `comfyui-anima-t8/` 仅作为本机参考实现，不进入公开提交。

## 验证

- `node --test tests\rhToolbox.test.ts tests\creativeDesk.test.ts tests\mediaMetadataDisplay.test.ts tests\canvasMouseInteractions.test.ts tests\aiWatermarkRunner.test.ts tests\comfyuiAppNodes.test.ts tests\comfyuiProvider.test.ts tests\updateMdRegressions.test.ts`
- `npm run type-check`
- `npm run rh-toolbox:check`
- `npm run public:check`
- `npm run build`
- `git diff --check`
- 浏览器本地预览冒烟：`http://localhost:4175/` 页面正常挂载，控制台无 error
